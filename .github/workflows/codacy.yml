# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow checks out code, performs a Codacy security scan
# and integrates the results with the
# GitHub Advanced Security code scanning feature.  For more information on
# the Codacy security scan action usage and parameters, see
# https://github.com/codacy/codacy-analysis-cli-action.
# For more information on Codacy Analysis CLI in general, see
# https://github.com/codacy/codacy-analysis-cli.

name: Codacy Security Scan

on:
  push:
    branches: ['master', 'main']
  pull_request:
    # The branches below must be a subset of the branches above
    branches: ['master']
  schedule:
    - cron: '31 16 * * 6'

permissions:
  contents: read

jobs:
  codacy-security-scan:
    permissions:
      contents: read # for actions/checkout to fetch code
    name: Codacy Security Scan
    runs-on: ubuntu-latest
    outputs:
      runs: ${{ steps.split.outputs.runs }}
    steps:
      # Checkout the repository to the GitHub Actions runner
      - name: Checkout code
        uses: actions/checkout@v5

      # Execute Codacy Analysis CLI and generate a SARIF output with the security issues identified during the analysis
      - name: Run Codacy Analysis CLI
        uses: codacy/codacy-analysis-cli-action@d840f886c4bd4edc059706d09c6a1586111c540b
        with:
          # Check https://github.com/codacy/codacy-analysis-cli#project-token to get your project token from your Codacy repository
          # You can also omit the token and run the tools that support default configurations
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          verbose: true
          output: results.sarif
          format: sarif
          # Adjust severity of non-security issues
          gh-code-scanning-compat: true
          # Force 0 exit code to allow SARIF file generation
          # This will handover control about PR rejection to the GitHub side
          max-allowed-issues: 2147483647

      # The Codacy CLI emits one SARIF "run" per tool it executed, all in a
      # single file. Since July 2025 the CodeQL upload action rejects multiple
      # runs sharing a category, so split them into one file per tool and let
      # the matrix job below upload each under its own category.
      - name: Split SARIF into one file per tool
        id: split
        run: |
          set -euo pipefail
          mkdir -p sarif-split
          total=$(jq '.runs | length' results.sarif)
          echo "SARIF contains $total run(s)"
          names='[]'
          for i in $(seq 0 $((total - 1))); do
            tool=$(jq -r --argjson i "$i" '.runs[$i].tool.driver.name // empty' results.sarif \
                   | tr -cd '[:alnum:]._-')
            [ -n "$tool" ] || tool="tool"
            name="${i}-${tool}"
            jq --argjson i "$i" \
               '{version: .version, "$schema": ."$schema", runs: [.runs[$i]]}' \
               results.sarif > "sarif-split/${name}.sarif"
            names=$(printf '%s' "$names" | jq -c --arg n "$name" '. + [$n]')
            echo "  -> sarif-split/${name}.sarif"
          done
          echo "runs=$names" >> "$GITHUB_OUTPUT"

      - name: Upload split SARIF as an artifact
        uses: actions/upload-artifact@v4
        with:
          name: codacy-sarif
          path: sarif-split
          retention-days: 1

  upload-sarif:
    name: Upload Codacy SARIF
    needs: codacy-security-scan
    if: needs.codacy-security-scan.outputs.runs != '[]'
    permissions:
      contents: read
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        run: ${{ fromJSON(needs.codacy-security-scan.outputs.runs) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Download split SARIF
        uses: actions/download-artifact@v4
        with:
          name: codacy-sarif
          path: sarif-split

      - name: Upload SARIF results file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: sarif-split/${{ matrix.run }}.sarif
          category: codacy-${{ matrix.run }}
